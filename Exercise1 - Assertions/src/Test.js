var Service = {
    greet: function (name) {
        return 'Hello ' + name + '!';
    }
};

describe("Service tests", function() { 
  it("should return a greeting with your name", function() {
    // write the test
  });
});