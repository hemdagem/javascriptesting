var calculator = {
  sum: function(no1, no2) {
      return no1 + no2;
  }
};

// specs code
describe("calculator", function() { 
  it("sum method should sum values", function() {
    // write the test
  });
});